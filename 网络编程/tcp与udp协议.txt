
应用层  python代码
传输层  与端口有关的都在这层 tcp udp
网络层  ip  路由器
数据链路层  mac  交换机  网卡(需要理解mac地址)
物理层


tcp
    打电话
        需要先建立连接然后才能通信
        占用连接（占用端口），可靠，但传输速度慢
udp
    发短信:你发出去了但不知道对方是否接收到
        不需要建立连接就可以通信
        不占用连接（不占用端口）不可靠，但是传输速度快